// ignore_for_file: constant_identifier_names

enum LanguageChange { English, Urdu, Spanish }

enum NewsType { allNews, topTrending }

enum SortByEnum {
  relevancy,
  popularity,
  publishedAt,
}


enum Status {loading,success,error}